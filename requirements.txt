# Cai torch/torchaudio ban CUDA RIENG truoc (khong nam trong file nay vi can --index-url
# phu hop voi driver CUDA dang co tren may). Xem HANDOFF.md muc 5, buoc 5.
# Vi du: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

faster-whisper>=1.0.3
whisperx>=3.3.0
# Pin these on Windows/GPU. ctranslate2 4.4.x looks for cuDNN 8 DLLs that are
# not shipped with the current CUDA/PyTorch setup, while numpy 2.x breaks DF.
ctranslate2==4.8.0
numpy<2.0
pyannote.audio>=3.3.0
deepfilternet>=0.5.6
python-dotenv>=1.0.1
huggingface_hub>=0.23.0

# Phase 3 - Streamlit App + job queue. streamlit>=1.41 vi UI dung
# st.fragment(run_every) + st.rerun(scope) (1.37), icon tren button (1.39),
# st.segmented_control (1.40).
streamlit>=1.41.0
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
celery>=5.4.0
redis>=5.0.4

# Phase 4 - Editor (bang chinh sua thay cho Custom Streamlit Component - xem
# HANDOFF.md Phase 4 ve ly do doi huong)
pandas>=2.2.0

# Phase 5 - Dich da ngon ngu (NLLB-200 qua transformers - xem canh bao rui ro
# trong subtitle_pipeline/infrastructure/translator_nllb.py)
transformers>=4.40.0
sentencepiece>=0.2.0

# Phase 5b - Long tieng (edge-tts - giong Neural TTS mien phi cua Microsoft,
# xem canh bao rui ro (can internet) trong
# subtitle_pipeline/infrastructure/tts_edge.py)
soundfile>=0.12.1
edge-tts>=6.1.9

# Wizard Upload - tai video tu URL (YouTube/Douyin/TikTok...) - xem
# subtitle_pipeline/infrastructure/downloader_ytdlp.py
yt-dlp>=2024.12.13

# Backend FastAPI cho UI React (backend/ + frontend/, quyet dinh 2026-07-03
# chuyen khoi Streamlit - xem HANDOFF.md)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic[email]>=2.9.0
pyjwt>=2.9.0
bcrypt>=4.1.0

# Phase 8 - Production storage (S3/MinIO)
boto3>=1.34.0
